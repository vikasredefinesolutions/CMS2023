/* eslint-disable no-unused-vars */
import React from 'react';
import { _ReviewDetailsProps } from './review';

const ReviewDetails_Type2: React.FC<_ReviewDetailsProps> = ({
  storeCode,
  reviewsCount,
  modalHandler,
  userId,
  productId,
  attributeId,
}) => {
  return <div>ReviewDetails_Type2</div>;
};

export default ReviewDetails_Type2;
